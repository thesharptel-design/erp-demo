/** Re-exports Sonner's imperative API. The legacy shadcn `toast` component was removed; use `<Toaster />` from `@/components/ui/sonner` in the tree. */
export { toast, type ExternalToast, type ToastT } from "sonner"
